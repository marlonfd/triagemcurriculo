info:
  name: GetCandidatesByJobPosition
  type: http
  seq: 11

http:
  method: GET
  url: "{{urlApi}}/candidates/job/1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
