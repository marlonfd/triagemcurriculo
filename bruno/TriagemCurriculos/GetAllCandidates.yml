info:
  name: GetAllCandidates
  type: http
  seq: 7

http:
  method: GET
  url: "{{urlApi}}/candidates"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
