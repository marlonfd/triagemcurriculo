info:
  name: DeleteCandidate
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{urlApi}}/candidates/1"
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
